# Only needed for DEVFLOW_PROVIDER=anthropic; the openai_compat
# backend speaks HTTP directly and needs no vendor SDK.
anthropic>=1.0.0
mcp>=2.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0
pydantic>=2.9.0
PyYAML>=6.0.2
python-dotenv>=1.0.1
pytest>=8.3.0
pytest-asyncio>=0.24.0
